// question 1:
console.log("name\"Sapnendra".length);

const sentence = 'The quick Brown fox jumps over the lazy dog.';
const  word = 'fox';
console.log(`The word "${word}" ${sentence.includes(word) ? 'is' : 'is now'} in the sentence`);